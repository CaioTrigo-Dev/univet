export const typography = {
  fonts: {
    // Font files not yet bundled — using system font as fallback.
    // To add custom fonts: npx expo install @expo-google-fonts/nunito @expo-google-fonts/dm-sans expo-font
    // and load them in App.tsx with useFonts().
    heading: undefined as unknown as string,
    subheading: undefined as unknown as string,
    body: undefined as unknown as string,
    bodyBold: undefined as unknown as string,
  },
  sizes: {
    xs: 12,
    sm: 14,
    md: 16,
    lg: 18,
    xl: 20,
    xxl: 24,
    h1: 32,
  },
};
